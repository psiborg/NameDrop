# NameDrop - Optional Dependencies
# Install with: pip install -r requirements.txt

# For EXIF date extraction from photos (JPEG, TIFF)
Pillow>=9.0.0

# For drag-and-drop file support
tkinterdnd2>=0.3.0

# Note: tkinter is usually included with Python installation
# If tkinter is missing on Linux, install via package manager:
#   Ubuntu/Debian: sudo apt-get install python3-tk
#   Fedora: sudo dnf install python3-tkinter
